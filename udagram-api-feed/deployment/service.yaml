apiVersion: v1
kind: Service
metadata:
  labels:
    app: microservices-feed
  name: microservices-feed
spec:
  ports:
  - name: "8080"
    port: 8080
    targetPort: 8080
  selector:
    app: microservices-feed
